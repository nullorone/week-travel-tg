import styled from 'styled-components';

export const Wrapper = styled.div`
    min-height: 100px;
    background-color: var(--tgui--skeleton);
    border-radius: 12px;
    box-shadow: 0 0 3px #282a2c;
`;
